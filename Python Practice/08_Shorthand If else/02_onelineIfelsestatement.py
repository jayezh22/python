# Assigning values to variables
a = 20  # assigning 20 to variable a
b = 10  # assigning 10 to variable b

# Conditional expression to print either "a" or "b"
print("a") if a < b else print("b")  # this will print "b" because a is not less than b