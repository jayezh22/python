# Assigning values to variables
a = 10  # assigning 10 to variable a
b = 20  # assigning 20 to variable b

# Conditional statement to check the relationship between a and b
if a < b:  # checking if a is LESS THAN b (not greater than)
    print("a is greater")  # this will never be true, because a is actually less than b
